import * as React from 'react';
import {Box, Typography} from '@mui/material';
import LinearProgress from '@mui/material/LinearProgress';


export const OtherTextSelect = (props) => {
    const {
        answer,
        ...other
    } = props;

    return (
    );
};
